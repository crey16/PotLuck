fastapi>=0.115,<1
pyjwt[crypto]>=2.9,<3
psycopg[binary,pool]>=3.2,<4
uvicorn>=0.30,<1
# Required by `uvicorn --env-file` (npm run api / dev:all), which reads
# .env.local. Without it uvicorn aborts at startup with ModuleNotFoundError.
python-dotenv>=1,<2
